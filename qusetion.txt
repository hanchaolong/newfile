1.epoll_wait
ret == 0(有数据就处理,没有数据3秒ret返回一次,)

2.  event.events = EPOLLIN;
   
    event.data.fd = sockfd;


一个结构体,上树一次,两个事件还是读事件中包含的套接字申请连接事件?

3.  memset(&event, 0, sizeof(event)); 
第二次申请的节点上树,是同一个树还是新的树\

4.continue
跳出本次循环,重新开始循环?


5.pub.h安装路径

6.int i=2;
while(1)
{if处理完 跳出else继续循环
if(i==2)
 {
   printf("%d",i);
 }
else
 {
  i++;
  }
}



7.回应浏览器报文是一个buf循环发送\r\n会不会出问题















